import bookTypes from "./book/types";
import climbTypes from "./climb/types";
import commentTypes from "./comment/types";
import locationTypes from "./location/types";

export default [bookTypes, climbTypes, commentTypes, locationTypes];
